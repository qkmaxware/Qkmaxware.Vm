use asm 1.0

// Create a value in the constant pool
@hello = "Hello World"

// The entrypoint 
.main
    load_const @hello   // Load a pointer to the string in the constant pool
    !printstr           // Macro to load the print the string to standard out

